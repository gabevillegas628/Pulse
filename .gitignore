node_modules/
dist/
.env
.env.local
*.log
*.tsbuildinfo
backend/prisma/seed.ts
backend/prisma/seeds/
Redesign/
01-thermodynamics.md
13-pentose-phosphate-pathway.md
docs/
.vscode/settings.json
migrate_structures_q.sql

# Question images the professor attached. User content, and in production it lives
# on a mounted volume rather than in the tree at all.
uploads/

# Dumps land here when backup-db.sh runs without a bucket; never in git.
backups/
restore/

# The key that decrypts the backups. Losing it costs a restore; committing it
# costs everything the backups hold. Broad on purpose: no key belongs in here.
*.key

# Console screenshot, kept locally for reference.
CloudFlareAPI.png

# Claude Code's workspace. settings.local.json is per-machine permission state,
# and nothing else in here has belonged to the repo either.
.claude/
